spring:
  # DataSource Config
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: 'jdbc:mysql://1.12.241.13:3306/user_center'
    username: dogyupi
    password: yupi666
  # session 失效时间
  session:
    timeout: 86400
server:
  address: 0.0.0.0
